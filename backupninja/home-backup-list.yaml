---
# determined by the command and packed manually:
# df | grep haven | sort -n -k4 -r | awk '{print $6}'
# view in nicer readable way:
# df -h | grep haven | sort -h -k4 -r

setup:
    log_directory: /var/log/ws-home-backup
remote:
    host: nas-master.csie.ntu.edu.tw
    port: 10734
    user: secret
    password: secret
    dest_base: csp_rsync/duplicity

backup_list:
  # 1
  - - /e/faculty
  # 2
  - - /e/alumni_master
  # 3
  - - /e/alumni_undergrad
  # 4
  - - /e/master
  # 5
  - - /e/alumni_inm_master
  # 6
  - - /e/alumni_inm_phd
  # 7
  - - /e/undergrad
  # 8
  - - /e/alumni_phd
  # 9
  - - /e/phd
  # 10
  - - /e/inm_phd
  # 11
  - - /e/inm_master
  # 12
  - - /e/ta
  # 13
  - - /e/course
  # 14
  - - /e/ra
  # 15
  - - /e/council
    - /e/os
  # 16
  - - /e/bebi_master
    - /e/bebi_phd
    - /e/select
  # 17
  - - /e/dept
    - /e/alumni_bebi_master
    - /e/visiting
    - /e/alumni_bebi_phd
    - /e/staff
